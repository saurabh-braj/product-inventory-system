export enum productColumns {
    PRODUCTID = 'productId',
    CATEGORY = 'category',
    NAME = 'name',
    DESCRIPTION = 'description',
    UNITS = 'units',
    ACTIONS = 'actions'
}

export const productGridColumns: string[] = [productColumns.PRODUCTID, productColumns.CATEGORY, productColumns.NAME, productColumns.DESCRIPTION, productColumns.UNITS, productColumns.ACTIONS];